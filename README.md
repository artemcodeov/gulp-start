# The most simple starter kit for gulp
Just clone it and type `npm install` or `yarn install`

### Features:
- SCSS support
- Hot Reloading
- Minifying CSS, Uglifying JS

### Usage:
Type `yarn gulp` for development
Type `yarn gulp build` for build